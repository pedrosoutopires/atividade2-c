#include <stdio.h>

int main(){

    int a, b, resultado;

    printf("escreva dois numeros inteiros: ");
    scanf("%d%d", &a, &b);

    if (a > b){
    resultado = a - b;
    printf("%d", resultado);
    } else
        if (a < b){
    resultado = b - a;
    printf("%d", resultado);
    }else
    printf("erro");

    return 0;

}
